import React, { Component } from 'react';
import {
  AppRegistry,
  StyleSheet,
  Text,
  View,
  Navigator
} from 'react-native';

export default class Login extends Component {
  render() {
    return (
        <Text>Testing the navigation router</Text>
    );
  }
}
